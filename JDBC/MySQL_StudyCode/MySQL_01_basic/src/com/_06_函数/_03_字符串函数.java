package com._06_函数;

// 字符串函数
//
// 1.常见字符串函数
//  ①字符串自身的操作
//     ·charset(str)：返回str的字符集
//     ·length(str)：返回字符串所占的字节长度【单位：字节】
//     ·char_length(str)：返回字符串的元素个数【单位：字符】
//     ·ucase(str)：将字符串转换为大写
//     ·lcase(str)：将字符串转换为小写
//      ltrim(str)：去除左边的空格
//      rtrim(str)：去除右边的空格
//      trim(str)：去除左右两端的空格
//
//  ②多个字符串间的操作
//     ·concat(str1, str2, str3, ...)：连接多个字符串
//      strcmp(str1, str2)：逐字符比较两字符串的大小，相等则返回0
//
//  ③关于子串的操作
//      instr(str, substring)：返回substring在str中出现的位置，没有则返回0【返回的位置=下标+1】
//      left(str, length)：从str的左边，取字符个数为length的子串
//      right(str, length)：从str的右边，取字符个数为length的子串
//     ·substring(str, position [,length])：从str的position开始，取字符个数为length的子串【position=下标+1】
//     ·replace(str, search_str, replace_str)：在str中用replace_str替换search_str
//
//
// 2.注意事项
//      以上字符串函数，只会返回操作结果，而不会改变表中的内容


public class _03_字符串函数 {
}
